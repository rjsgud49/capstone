name: Auto Git Pull to GCP

on:
  push:
    branches:
      - main

jobs:
  git-pull:
    runs-on: ubuntu-latest

    steps:
      - name: Pull latest code on GCP VM
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: 34.64.198.39
          username: ubuntu
          key: SHA256:R3gMew63jac+Ge5rxsLWi6uQlH/ReTCkTW/NDliPG7M
          script: |
            cd ~/capstone
            git pull origin main
